
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды 
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
    //+Валера добавил по задаче 1.3
	ДопФункц_ДобавлениеНовыхРеквизитовНаФорму.ДобавитьРеквизитКонтактноеЛицоНаФорму(ЭтаФорма);
	
	ЭлементФормы = Элементы.Добавить("СогласованнаяСкидка", Тип("ПолеФормы"), Элементы.ЯчейкаСуммаДокумента);
	ЭлементФормы.Вид = ВидПоляФормы.ПолеВвода;
	ЭлементФормы.ПутьКДанным = "Объект.ДопФункц_СогласованнаяСкидка";
	ЭлементФормы.УстановитьДействие("ПриИзменении", "ПриИзмененииСоглСкидки");
	
	Команда = Команды.Добавить("ПересчитатьСуммуДокумента");
	Команда.Заголовок = "Пересчитать сумму документа";
	Команда.Действие = "КомандаПересчитатьСуммуДокумента";
	
	КомандаФормы = Элементы.Добавить("КнопкаПересчитатьСуммуДокумента", Тип("КнопкаФормы"), Элементы.ЯчейкаСуммаДокумента);
	КомандаФормы.ИмяКоманды = "ПересчитатьСуммуДокумента";
	КомандаФормы.Вид = ВидКнопкиФормы.ОбычнаяКнопка;
	//-
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
    // СтандартныеПодсистемы.ПодключаемыеКоманды
    ПодключаемыеКомандыКлиент.НачатьОбновлениеКоманд(ЭтотОбъект);
    // Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
КонецПроцедуры

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
    // СтандартныеПодсистемы.ПодключаемыеКоманды
    ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Объект);
    // Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
    ПодключаемыеКомандыКлиент.ПослеЗаписи(ЭтотОбъект, Объект, ПараметрыЗаписи);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыТовары

&НаКлиенте
Процедура ТоварыКоличествоПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыЦенаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыСкидкаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыПриИзменении(Элемент)
	
	РассчитатьСуммуДокумента();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыУслуги

&НаКлиенте
Процедура УслугиКоличествоПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура УслугиЦенаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура УслугиСкидкаПриИзменении(Элемент)
		
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);

КонецПроцедуры

&НаКлиенте
Процедура УслугиПриИзменении(Элемент)
	
	РассчитатьСуммуДокумента();

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура РассчитатьСуммуСтроки(ТекущиеДанные)
	
	//+Убрал это по задача 1.3
	//КоэффициентСкидки = 1 - ТекущиеДанные.Скидка / 100;
	//ТекущиеДанные.Сумма = ТекущиеДанные.Цена * ТекущиеДанные.Количество * КоэффициентСкидки;
	//-
	
	//+Валера добавил по задаче 1.3
	Если ТекущиеДанные.Скидка + Объект.ДопФункц_СогласованнаяСкидка < 100 Тогда
		СуммаСтроки = ТекущиеДанные.Цена * ТекущиеДанные.Количество;
		ТекущиеДанные.Сумма = СуммаСтроки - (СуммаСтроки / 100 * Объект.ДопФункц_СогласованнаяСкидка + ТекущиеДанные.Скидка);
	Иначе
		ТекущиеДанные.Сумма = 0;
		Сообщить("Общая скидка превышает 100%");
	КонецЕсли;
	//-
	
КонецПроцедуры

&НаКлиенте
Процедура РассчитатьСуммуДокумента()
	
	Объект.СуммаДокумента = Объект.Товары.Итог("Сумма") + Объект.Услуги.Итог("Сумма");
	
КонецПроцедуры

#Область ПодключаемыеКоманды

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
    ПодключаемыеКомандыКлиент.НачатьВыполнениеКоманды(ЭтотОбъект, Команда, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПродолжитьВыполнениеКомандыНаСервере(ПараметрыВыполнения, ДополнительныеПараметры) Экспорт
    ВыполнитьКомандуНаСервере(ПараметрыВыполнения);
КонецПроцедуры

&НаСервере
Процедура ВыполнитьКомандуНаСервере(ПараметрыВыполнения)
    ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, ПараметрыВыполнения, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
    ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Объект);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

#КонецОбласти

#КонецОбласти 

#Область ДоработкиВалеры

//+Доработки от Валеры по задаче 1.3
&НаКлиенте
Процедура КомандаПересчитатьСуммуДокумента(Команда) 
	//Доработки от Валеры по задаче 1.3
	ПересчитатьСуммуДокумента();
КонецПроцедуры

&НаКлиенте
Процедура ДопФункцРассчитатьСуммуСтроки()
	//Доработки от Валеры по задаче 1.3
	Если Объект.ДопФункц_СогласованнаяСкидка <> 0 Тогда
		Оповещение = Новый ОписаниеОповещения("ПослеЗакрытияВопроса", ЭтотОбъект, Параметры);
		ПоказатьВопрос(Оповещение, "Применить согласованную скидку?", РежимДиалогаВопрос.ДаНет);	
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗакрытияВопроса(Результат, Параметры) Экспорт
	//Доработки от Валеры по задаче 1.3
	Если Результат = КодВозвратаДиалога.Нет Тогда
		Возврат;
	Иначе
		ПересчитатьСуммуДокумента();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриИзмененииСоглСкидки()
	//Доработки от Валеры по задаче 1.3
	ДопФункцРассчитатьСуммуСтроки();
КонецПроцедуры 

Процедура ПересчитатьСуммуДокумента()
	//Доработки от Валеры по задаче 1.3
	Для Каждого СтрТовар Из Объект.Товары Цикл
		Если СтрТовар.Скидка + Объект.ДопФункц_СогласованнаяСкидка < 100 Тогда
			СуммаСтроки = СтрТовар.Цена * СтрТовар.Количество;
			СтрТовар.Сумма = СуммаСтроки - (СуммаСтроки / 100 * Объект.ДопФункц_СогласованнаяСкидка + СтрТовар.Скидка);
		Иначе
			СтрТовар.Сумма = 0;
			Сообщить("Общая скидка превышает 100%");
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого СтрУслуги Из Объект.Услуги Цикл 
		Если СтрУслуги.Скидка + Объект.ДопФункц_СогласованнаяСкидка < 100 Тогда
			СуммаСтроки = СтрУслуги.Цена * СтрУслуги.Количество;
			СтрУслуги.Сумма = СуммаСтроки - (СуммаСтроки / 100 * Объект.ДопФункц_СогласованнаяСкидка + СтрУслуги.Скидка);
		Иначе
			СтрУслуги.Сумма = 0;
			Сообщить("Общая скидка превышает 100%");
		КонецЕсли;
	КонецЦикла; 
	
	Объект.СуммаДокумента = Объект.Товары.Итог("Сумма") + Объект.Услуги.Итог("Сумма");
КонецПроцедуры
//-                                  
#КонецОбласти 
